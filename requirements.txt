# DAMO 699 capstone. The stages that need no GPU.
#
# Set up with uv:
#     uv venv --python 3.12 .venv
#     uv pip install --python .venv/Scripts/python.exe -r requirements.txt
#
# Or with pip:
#     python -m venv .venv
#     .venv/Scripts/activate
#     pip install -r requirements.txt
#
# Tested on Python 3.12.13 with numpy 2.5.2, pandas 3.0.5, scipy 1.18.0,
# pyarrow 25.0.1, matplotlib 3.11.1, statsmodels 0.14.6, pytest 9.1.1.

numpy>=1.26
pandas>=2.1
scipy>=1.11
pyarrow>=14.0
matplotlib>=3.8
seaborn>=0.13
statsmodels>=0.14
pytest>=7.4
tqdm>=4.66

# AgentFormer needs its own environment. It pins an old PyTorch.
# Do not add torch here. See scripts/setup_agentformer.py.
